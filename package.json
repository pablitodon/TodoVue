{
    "name": "todo-crm-app",
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "build": "yarn workspaces run build",
      "start": "yarn workspaces run start",
      "start:front": "yarn workspace todo-crm run dev",
      "start:back": "yarn workspace todo-app run dev"
    }
  }